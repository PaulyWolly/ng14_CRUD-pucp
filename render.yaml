# Optional Render Blueprint — or create the Web Service manually in the dashboard.
services:
  - type: web
    name: ng14-crud-json-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start:api
    healthCheckPath: /logged-in-users
