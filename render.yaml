# Render Web Service for the json-server API (login/CRUD backend)
services:
  - type: web
    name: ng14-crud-json-api
    runtime: node
    plan: free
    rootDir: api
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "18"
